# Prevent automatic dependency updates
save-exact=true
package-lock=true
audit=false
fund=false

# Prevent npm from auto-installing peer dependencies
legacy-peer-deps=true

# Disable automatic update checks
update-notifier=false

# Disable npm version check
node-version-check=false

# Disable npm security warnings
no-audit=true
